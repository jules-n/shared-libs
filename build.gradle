subprojects {
    apply plugin: 'java'
    apply plugin: 'maven'

    repositories {
        mavenLocal()
        mavenCentral()
    }
}

wrapper {
    gradleVersion = project['gradle.version']
    distributionType = 'ALL'
}
